# 关机恢复指南

> 更新: 2026-06-16

## 当前可以关机

- 所有 192 个实验已完成
- JSON 结果全量同步到本地 (`results/logs/`)
- 模型权重大部分同步 (`checkpoints/autodl/`)
- 代码已 push 到 GitHub

## 关机步骤

直接在 AutoDL 控制台关机。

## 恢复步骤

1. **开机**: AutoDL 控制台启动实例（需 GPU 模式）
2. **检查**: SSH 连接确认
3. **同步**: 拉取最新数据
   ```bash
   python scripts/tmp_paramiko_autodl_runner.py --pull-all
   python scripts/download_checkpoints.py
   ```

## 需要 GPU 的操作

- 训练新实验
- 生成混淆矩阵（需加载模型权重推理）
- 重新提取 XAI / 诊断数据

## 不需要 GPU 的操作

- 查看 JSON 结果
- 更新论文/图表/文档
- 代码修改
