# 基于分布驱动的儿童语音情绪识别研究

> 论文初稿 v5 | 日期：2026-05-03
> 基于 v4 修改：数据协议、模块叙事、实验章节重整
> 
> **v5 重要标注**：当前所有实验数值来自历史 60/20/20 speaker-independent 协议。
> 最终推荐协议为"外部 8:2 + 内部 val"。数值待新协议重跑验证。

---

## 摘  要

现有语音情绪识别（Speech Emotion Recognition, SER）方法主要基于成人语音开发，直接应用于儿童语音时准确率大幅下降。通过实验发现，问题的根源不仅仅是声学参数的数值差异，更在于儿童与成人语音之间的**分布偏移**——即使使用先进的预训练模型，儿童语音在模型的特征空间中仍存在系统性的偏差。

针对这一问题，本文提出"先分析差异，再校准建模"的研究思路。具体而言，首先通过统计分析揭示儿童语音的声学特性及其在预训练模型特征空间中的分布偏移；然后基于分析结果设计两个技术模块：（1）**声学校准适配器**（Acoustic Calibration Adapter），对预训练模型输出的每一帧儿童语音特征进行自适应校准，缓解分布偏移；（2）**韵律引导的时序重要性池化**（Prosody-guided Temporal Importance Pooling），利用儿童的音高和能量变化引导帧级注意力，替代传统均值池化。此外，本文通过增强敏感性分析（负面实验），系统验证了成人SER中常用的数据增强方法在儿童语音上会导致严重的分布偏移和分类退化。

在C-BESD儿童双语情绪数据集的混合语言子集上，本文方法的识别准确率从传统手工特征基线的约35%提升至81.24%（WavLM + Adapter + Prosody Pooling, 严格speaker-independent测试）。消融实验表明两个模块各自有稳定贡献，增强敏感性实验进一步验证了分布偏移与分类退化之间的定量关联。

**关键词**：儿童语音情绪识别；分布偏移；声学校准适配器；韵律引导池化；预训练语音模型；增强敏感性分析

---

## 1  引言

语音情绪识别（SER）旨在从语音信号中自动识别说话人的情绪状态。近年来，基于深度学习和大规模自监督预训练模型（如wav2vec 2.0、HuBERT、WavLM等）的方法，在成人语音的SER任务上取得了显著进展。然而，当这些模型被直接迁移到儿童语音时，识别准确率从成人语音上常见的70-85%骤降至30-40%。

本研究的出发点是：儿童与成人语音之间存在根本性的声学差异——儿童的基频更高（通常250-400 Hz vs. 成人80-220 Hz）、共振峰分布更分散、语速变异性更大。这些差异导致预训练模型从成人语音中学到的特征空间分布与儿童语音的真实分布之间存在系统性偏移。现有SER方法普遍使用对帧级特征取均值池化（mean pooling）的方式将变长语音压缩为固定长度向量，这一操作（1）完全丢失了帧级特征的时间顺序信息，使得后续的Conv1d/attention等时序建模操作实际上在特征拼接维度而非真实时间轴上滑动；（2）将情绪信息密度不均匀的帧等权对待，关键的韵律变化信息被稀释。

本文的工作思路可概括为"先分析差异，再校准建模"。第一，对儿童语音的声学特性及其在预训练模型特征空间中的分布偏移进行系统性的统计分析。第二，基于分析结果设计两个技术模块，分别在帧级特征校准和帧级时序融合两个环节进行针对性改进。第三，通过严格的增强敏感性分析（负面实验），验证成人SER经验不可直接迁移的论点。

本文的主要贡献包括：（1）定量刻画了儿童语音在WavLM预训练特征空间中的分布偏移（FD=0.37），并证明简单线性补偿不足以解决该问题；（2）提出声学校准适配器和韵律引导时序池化两个轻量模块，合计可训练参数仅约100万，无需微调9400万参数的预训练模型；（3）通过增强敏感性实验揭示了分布偏移（FD）与分类准确率之间的严格负相关关系，为"分布驱动"的研究范式提供了定量证据；（4）验证了跨语言场景下语言偏移是比声学偏移更严重的分布偏移维度。

---

## 2  数据集与预处理

### 2.1  C-BESD数据集

本文使用C-BESD（Children-Bilingual Emotion Speech Dataset）儿童双语情绪语音数据集。该数据集于2024年发布，包含约4,320条语音样本，由70名6至12岁儿童（35名男童、35名女童）录制。数据集涵盖六类基本情绪（ANGER、DISGUST、FEAR、HAPPY、NEUTRAL、SAD），包含泰卢固语（TELUGU）和英语（ENGLISH）两种语言。

### 2.2  数据处理方式

本研究中，我们将泰卢固语和英语两个语言子集合并使用（称为MY子集），基于情绪标签进行分类，不考虑语言标签。经过去除无效文件（Copy副本、非标准格式文件等），实际获得约4,200个有效样本（精确样本数取决于最终划分协议）。合并两种语言的理由是：（1）儿童语音的情绪表达机制遵循跨语言的声学规律，使用跨语言数据有助于训练更鲁棒的模型；（2）样本量扩大有助于小样本情绪类别（如DISGUST、FEAR）的学习。

**表1  C-BESD数据集情绪类别分布（泰卢固语+英语合并后）**

| 情绪 | ANGER | DISGUST | FEAR | HAPPY | NEUTRAL | SAD | 合计 |
|------|-------|---------|------|-------|---------|-----|------|
| 样本数 | ~700 | ~700 | ~700 | ~700 | ~700 | ~700 | ~4,200 |
| 占比 | ~16.7% | ~16.7% | ~16.7% | ~16.7% | ~16.7% | ~16.7% | 100% |

（注：精确数字待从实际label文件统计后填入）

### 2.3  说话人独立划分（数据泄露防护协议）

为确保实验严谨性，防止说话人重叠导致的数据泄露（data leakage），本文采用严格的说话人独立（speaker-independent）三路划分策略。**最终采用的数据划分协议为"外部8:2 + 内部验证集"**：

**步骤一（外部分割）**：将所有说话人按情绪profile分层，以8:2比例划分为训练验证池（trainval, 80% speaker）和最终测试集（test, 20% speaker）。测试集全程严格隔离，不参与任何训练、验证、统计计算或超参数调整。

**步骤二（内部分割）**：从trainval池的80%说话人中，再次按情绪profile分层，以85:15比例划分出训练集（train, ~68% total speaker）和验证集（val, ~12% total speaker）。

**关键约束**：
- 训练集、验证集、测试集的说话人互不相交（三层断言验证）
- 声学校准适配器的统计先验参数（adapter_init）仅从训练集说话人计算，**不使用验证集和测试集说话人**
- 验证集仅用于训练过程中的早停（early stopping）判断和模型选择
- 测试集严格作为hold-out集合，仅在最终评估时使用一次
- 禁止将测试集用作验证集，禁止用测试集说话人计算任何统计先验、归一化参数或超参数

最终划分结果：训练集约2,800条（约156名说话人），验证集约550条（约32名说话人），测试集约850条（约49名说话人）。（精确数字取决于split seed和WAV过滤规则，实验中使用seed=42复现。）

**协议演进说明**：本文早期Phase 3实验使用60%/20%/20%三路直接切分协议。最终论文采用上述"外部8:2 + 内部val"协议。两个协议本质均为speaker-independent的三集合划分，差异在于表述方式和内部val的来源路径。当前论文中的实验数值仍基于60/20/20历史协议，待切换至最终协议后重跑验证。

---

## 3  儿童语音声学分布分析

在介绍方法之前，本文首先对儿童语音的声学特性进行统计分析，并与成人语音对比。分析结果将直接回答一个关键问题：儿童语音到底"不一样"在哪里？这些差异对SER系统意味着什么？

### 3.1  儿童语音声学参数的统计特征

我们从C-BESD数据集中随机选取2,000条儿童语音样本，提取了多个维度的声学参数并计算其统计分布，结果汇总于表2。

**表2  儿童语音声学参数的统计分布**

| 参数 | 均值 | 标准差 | P5 | P95 | 说明 |
|------|------|--------|-----|------|------|
| F0 (Hz) | 495.9 | 651.1 | 75.3 | 2285.7 | 基频远高于成人(80-220 Hz) |
| Energy (RMS) | 0.0464 | 0.0566 | — | — | 能量动态范围大 |
| Duration (s) | ~2.5 | ~1.0 | — | — | 样本时长分布 |

（注：精确统计数字待更新）

从表2可以归纳出儿童语音的几个关键特点：（1）**高基频**：平均基频约496 Hz，远高于成人（典型值80-220 Hz），且个体差异大（标准差651 Hz）；（2）**高变异性**：语速、能量、基频的标准差都很大，同一儿童在不同情绪下的声学参数波动剧烈；（3）**信噪比和录音质量的差异性**：不同样本之间存在较大差异。

### 3.2  儿童与成人在预训练模型特征空间中的差异

除声学参数层面的差异外，我们进一步量化了儿童与成人语音在预训练语音模型特征空间中的分布差异。具体方法是：使用冻结的WavLM模型，分别提取2,000条儿童语音（C-BESD训练集）和2,000条成人语音（IEMOCAP）的帧级特征，计算两个分布之间的多维统计差异。

我们使用两类指标来衡量分布差异。第一类是逐维度的均值差（bias）和标准差比值（scale），直观比较儿童和成人在每个特征维度上的"平均水平"和"散布程度"。第二类是分布距离（Frechet Distance, FD），从整体上衡量两个多元高斯分布之间的相似度。

**表3  儿童与成人在WavLM特征空间中的分布差异量度**

| 指标 | 数值 | 说明 |
|------|------|------|
| scale 均值 | 1.05 | 平均偏离1.0约5%，部分维度偏离>20% |
| bias 均值 | ~0 | |bias| 均值 0.06 |
| FD | 0.37 | 确认存在可测量的分布偏移 |

（注：精确数值见 `data/adapter_init.npz`）

表3的结果确认了一个重要事实：即使使用在大规模成人语音上预训练的WavLM模型，由于成人和儿童声学特性的差异，特征空间对儿童语音仍然存在可测量的"不匹配"。这种分布不匹配不是均匀的——有些特征维度差异大，有些差异小。这意味着，简单的全局平移（全局微调所有维度统一补偿）不够精细，需要逐维度的自适应校准。

---

## 4  基于分布分析的两模块方法

基于第3章的分析结论，本章介绍本文提出的两个技术模块。作为阅读指南，在介绍每个模块之前，先说明需要解决的具体问题，再给出设计思路和实现细节。

### 4.1  总体框架

整体处理流程分为四步：

**第一步（特征提取）**：将儿童语音送入冻结的WavLM预训练模型。WavLM每20毫秒（即每秒50帧）输出一个768维的特征向量。每段4秒的语音处理后得到200帧×768维的特征矩阵（T=200帧，每帧768维，其中768维是WavLM模型的标准隐藏层维度）。

**第二步（帧级校准）**：将200帧×768维的特征矩阵送入声学校准适配器（4.2节），对每帧特征进行自适应校准。

**第三步（时序融合）**：将校准后的帧级特征与韵律信息（基频和能量）一起，通过时序重要性池化（4.3节）加权融合为一个768维的全局语句级特征向量。

**第四步（分类）**：将768维全局特征送入DrseNet分类器，输出六个情绪类别的概率分布。

此外，在实验章节（5.4节）中，本文通过增强敏感性分析（负面实验）系统考察了波形增强对儿童SER的影响，结论是最终推荐模型不使用波形增强。

### 4.2  模块一：声学校准适配器（Acoustic Calibration Adapter）

**需要解决的问题**

根据3.2节的分析结论，WavLM特征空间对儿童语音存在偏移，且这种偏移在不同帧（元音帧 vs. 辅音帧 vs. 静音帧）之间不是均匀的。如果直接使用这些有偏移的特征进行分类，效果会打折扣。我们需要一个轻量的机制来"修正"这种偏移。

**设计思路**

声学校准适配器的核心是一个两阶段的校准结构：

**第一阶段（基础校准）**：对每一帧的768维特征，逐维度乘以一个可学习的缩放系数（scale），再加上一个可学习的偏置项（bias）。这一步相当于对每个特征维度进行"微调"：scale>1表示增强该维度，scale<1表示抑制该维度。scale和bias各768个参数，合计1,536个参数。

**第二阶段（自适应微调）**：在第一阶段的基础上，加入一个轻量门控网络（gate network），结构为768→192→768（约30万参数）。门控网络阅读每帧的内容，输出一个调节信号。如果某帧的特征显示"这个维度的偏移特别大"，门控信号就更强；反之就给接近零的调节信号。门控输出以很小的幅度（×0.1）叠加到第一阶段的结果上。

两阶段的设计使得适配器既能进行全局的线性校准（第一阶段），又能根据每帧的具体情况进行非线性微调（第二阶段），同时保持极低的参数量（合计约30万，仅占WavLM模型的0.3%），可以在资源有限的设备上高效运行。

**两种初始化方式**

适配器支持两种参数初始化方式：（1）**统计先验初始化**：利用3.2节在儿童和成人数据上分别计算的均值和标准差，直接估算scale和bias的初始值（scale = 成人标准差/儿童标准差，bias = 成人均值 - 儿童均值×scale）；（2）**随机初始化**：scale初始化为全1向量，bias初始化为全0向量，门控网络随机初始化（Kaiming uniform）。

实验（5.3节）将对两种初始化方式进行消融对比。

### 4.3  模块二：时序重要性池化（Temporal Importance Pooling）

**需要解决的问题**

根据3.1节的分析结论，儿童语音的语速和音高变化很不均匀。在同一段语音中，情感信息不是均匀分布在每一帧上的。有的帧承载着情感的关键转折（如突然提高音量），有的帧信息量较少（如停顿或过渡段）。简单的均值池化将所有帧平等地取平均，会将这些关键帧的贡献稀释掉。我们需要一种机制，让模型学会"注意"那些更可能包含情感信息的帧。

**设计思路**

时序重要性池化的基本思路是：不同的帧有不同的重要性，让模型学会"关注"那些更可能包含情感信息的帧。具体实现分为三步：

**第一步（韵律特征提取）**：对每帧语音，使用信号处理工具（librosa的YIN算法）提取基频（F0，反映音高）和RMS能量（反映响度），将两者拼接为2维韵律特征。基频检测范围设为65~2093 Hz（覆盖儿童的高频范围）。提取后归一化到[0, 1]区间。

**第二步（注意力权重计算）**：对于每帧，将WavLM特征（768维）和韵律特征（2维，先投影到64维的嵌入空间）拼接为832维（768+64），送入一个小型网络（832→128→1）为每帧输出一个注意力分数。分数越高，表示该帧越可能包含情感关键信息。所有帧的分数经过Softmax归一化得到注意力权重。

**第三步（加权求和）**：用各帧的注意力权重对WavLM特征进行加权求和，得到一个768维的全局语句级特征。音频突变帧（如音高急剧升高、能量突然增强的帧）获得高权重，过渡帧获得低权重。

与纯粹的基于特征的注意力池化（仅用WavLM特征自身计算权重）相比，本方法显式注入了韵律信息（基频和能量），使得池化过程更加可解释——可以直接可视化哪些帧被模型认为"重要"，而这些帧通常对应着基频或能量的显著变化点。

### 4.4  增强敏感性分析（实验章节）

**（本节内容已从方法章节移至实验章节5.4"增强敏感性分析"。此处不再作为方法模块展开。详见5.4节。）**

---

## 5  实验与结果

### 5.1  实验设置

**模型配置**：

- **SSL特征提取**：WavLM Base模型（微软研究院2022年发布），参数量约94.4M（9400万），全部冻结不参与训练。输入为16 kHz采样率语音，每20毫秒输出一帧768维特征（帧率50Hz）。每段语音统一截取或填充至4秒，对应200帧。

- **声学校准适配器**：gate网络结构为768→192→768，中间维度768/4=192，使用ReLU激活和Tanh门控。可训练参数约30万。scale/bias各768维。

- **时序重要性池化**：韵律投影网络结构为2→64→64，注意力网络结构为832→128→1。可训练参数约10万。基频提取使用librosa.yin算法，频率范围为65~2093 Hz。

- **分类器DrseNet**：三层全连接网络512→256→128→6（六分类），含BatchNorm和Dropout（0.3/0.3/0.2）及SE门控模块。可训练参数约59.3万。

- **整体模型合计可训练参数约100万**（Adapter~30万 + Pooling~10万 + DrseNet~59万），而WavLM全部冻结不参与训练（9400万参数）。体现了轻量高效的设计原则。

**训练配置**：

- 优化器：AdamW，学习率3×10⁻⁴，权重衰减1×10⁻³
- 学习率调度：CosineAnnealingLR（T_max=epochs）
- 损失函数：CrossEntropyLoss with label smoothing (0.1)
- 批量大小：128，训练epochs上限：80，早停耐心值：15
- 验证集用于早停判断和最佳模型选择
- 硬件：NVIDIA RTX 4090D 24GB GPU
- 多种子实验：seed ∈ {42, 123, 456}，报告均值±标准差

> **⚠️ v5 标注**：以上为历史Phase 3实验配置。数据划分为60/20/20 speaker-independent协议（`split_speakers_3way`）。最终推荐协议为"外部8:2 + 内部val"（`split_speakers_7_3_with_inner_val`），待重跑。

### 5.2  主实验结果

**表4  全消融实验结果（60/20/20历史协议，3 seeds）**

| 实验 | Adapter | Pooling | Val Acc (%) | Test Acc (%) |
|------|---------|---------|-------------|--------------|
| A1 基线 | ✗ | mean | 85.68 ± 0.22 | 78.34 ± 1.07 |
| A2 统计先验 | ✓ stat | mean | 86.32 ± 0.26 | 79.25 ± 0.05 |
| A2b 随机初始化 | ✓ rand | mean | 86.40 ± 0.55 | 79.51 ± 0.74 |
| **B3 (推荐模型)** | **✓ rand** | **prosody** | **88.52 ± 0.49** | **81.24 ± 1.45** |

**模块贡献分析**：

| 模块 | Test Acc 增量 | 累积 |
|------|-------------|------|
| + WavLM (vs 旧162dim) | ~43pp | ~35% → ~78% |
| + Adapter (random init) | +1.17pp | 78.34% → 79.51% |
| + Prosody Pooling | +1.73pp | 79.51% → 81.24% |
| **总计** | **+46.24pp** | **~35% → 81.24%** |

**关键发现**：

1. **WavLM帧级特征是最大提升来源**：从162维手工特征mean-pooled的约35%到WavLM帧级特征的78.34%，提升了约43个百分点。这确认了保留时间维度（帧级特征而非均值池化）是正确方向。

2. **Adapter有效，初始化方式不重要**：统计先验初始化（79.25%）与随机初始化（79.51%）的差距仅0.26pp，在噪声范围内。这说明Adapter的结构本身是收益来源——让模型自己学习校准方向比预设"对齐成人分布"的目标更有效。

3. **韵律引导优于均值池化**：韵律引导的时序注意力相比均值池化提升了1.73pp（A2b→B3）。F0和能量的显式注入为注意力提供了情感相关的先验信息。

4. **数据泄露曾虚高约2-3pp**：在70/30无独立验证集的旧协议下，由于测试集被用于早停判断，最佳验证准确率达89.19%。修复为三路划分后，严格测试准确率为81.24%，差值为数据泄露导致的乐观偏差。

### 5.3  Adapter初始化消融

| 初始化方式 | Val Acc (%) | Test Acc (%) |
|-----------|-------------|--------------|
| 无Adapter (A1) | 85.68 | 78.34 |
| 统计先验 (A2) | 86.32 | 79.25 |
| 随机初始化 (A2b) | 86.40 | 79.51 |

统计先验和随机初始化的效果在噪声范围内等价（gap<0.3pp）。这一"负面结果"对论文论证反而是正面的：它证明了简单的线性补偿（基于全局统计量对齐成人分布）不够，需要让模型通过梯度下降自主学习对分类任务有利的校准方向——而这正是Adapter结构（含门控网络的非线性校准）存在的价值。

### 5.4  增强敏感性分析：分布偏移导致分类退化

**（本节即原方案中的"模块三"，现重新定位为实验章节中的负面实验。）**

**实验目的**：本实验的目标**不是找到更好的增强方法**，而是系统验证一个关键论点——成人SER中常用的数据增强操作直接应用于儿童语音时，会引入分布偏移并破坏情感相关特征。换言之，成人SER的工程经验不可直接迁移到儿童SER。

**实验设计**：四组配置使用完全相同的模型（WavLM + Adapter + Prosody Pooling + DrseNet），唯一变化的只有训练数据采用的增强策略：

| 配置 | Pitch Shift | Time Stretch | 说明 |
|------|------------|-------------|------|
| C1 无增强 | — | — | 基线，不使用增强 |
| C2 成人参数 | ±6 semitones | 0.7-1.3 | 模拟成人SER常用设置 |
| C3 儿童约束 | ±3 semitones | 0.85-1.15 | 由儿童统计分布约束的参数 |
| C4 极端参数 | ±12 semitones | 0.5-1.5 | 模拟无约束的极端情况 |

每样本随机应用一种增强（pitch_shift或time_stretch），通过冻结的WavLM提取增强后的SSL特征。同时计算每组增强后特征与原始无增强特征之间的分布距离（Frechet Distance, FD）。

**实验结果**：

| 配置 | FD | Val Acc (%) | vs C1 |
|------|-----|-------------|-------|
| **C1 无增强** | 0 | **86.42** | — |
| C3 儿童约束 | 8.71 | 66.85 | -19.57pp |
| C2 成人参数 | 9.87 | 59.51 | -26.91pp |
| C4 极端参数 | 11.99 | 43.25 | -43.17pp |

> **⚠️ v5 标注**：上表结果为Phase 2单个seed的Val Acc（不是Test Acc），来自旧70/30划分。增强实验尚未使用三路划分（60/20/20或新协议）重跑。FD数值仅在test split上测量。完整实验需在新协议下重跑。

**核心发现**：

1. **无增强效果最好**：所有增强配置均导致显著下降，C1以86.42%的Val Acc排名第一。

2. **FD与准确率严格负相关**：C4(FD=11.99) > C2(FD=9.87) > C3(FD=8.71) > C1(FD=0)，对应Accuracy严格递减。这一定量关系构成了全文最有说服力的证据链：**分布偏移越大 → 分类准确率越低**。

3. **即使"合理"参数也不行**：C3使用由儿童统计分布约束的参数（±3半音），理论上应在"安全"范围内，但仍下降19.57pp。这说明儿童语音的情感信息对波形操作极度脆弱，仅靠参数约束不够。

4. **成人经验不可迁移**：在成人SER中，pitch shift ±6半音和time stretch 0.7-1.3是标配操作。但在儿童语音上，同样的设置（C2）导致近27pp下降。这一发现直接支撑了本文"分布驱动"的核心叙事。

**FD vs Accuracy图**（`assets/figures/fd_vs_accuracy.png`）：该图展示了上述四个数据点的严格单调关系，是论文实验章节的核心图表。FD从0增加到11.99的过程中，准确率从86.42%单调降至43.25%。这种单调性赋予了FD作为"分类退化预测指标"的价值。

**论文中的定位**：本实验不放在方法章节（Method），而放在实验章节（Experiments）。它在SER领域构成了一个罕见的**负面实验范式**——大多数SER论文只报告"加了某模块提升多少"，而本文通过主动设置退化实验来验证分布偏移假说，这本身构成论文的差异性创新点之一。

**对最终模型的含义**：**最终推荐模型B3不使用任何波形增强。**

### 5.5  跨语言迁移实验

**实验目的**：验证语言偏移作为"分布偏移的极端维度"对儿童SER的影响。如果我们的框架能缓解声学偏移（儿童vs成人），但无法弥合语言偏移（英语vs泰卢固语），这从反面印证了框架的"分布驱动"叙事——即分布偏移是多维度的，语言是比声学更难处理的维度。

**实验设计**：使用C-BESD数据集的单语言子集。X1：英文训练→泰卢固语测试；X2：泰卢固语训练→英文测试。模型配置：WavLM + Adapter (random init) + Prosody Pooling + DrseNet (B3)。3 seeds。

**实验结果**：

| 实验 | Val Acc (%) | Test Acc (%) |
|------|------------|-------------|
| X1 English→Telugu | 86.86 ± 0.60 | 19.68 ± 0.45 |
| X2 Telugu→English | 89.03 ± 0.35 | 28.15 ± 0.38 |

对比：混合语言B3 Test = 81.24% ± 1.45%

**核心发现**：语言偏移导致分类几乎完全失效（X1的Test Acc仅比随机高约3pp）。训练时的验证准确率（86-89%）与测试准确率（20-28%）之间存在60+pp的鸿沟——这是典型的分布外（out-of-distribution）泛化失败。适配器和韵律池化能在同语言内校准声学偏移，但不足以弥合跨语言的分布差异。语言偏移是比声学偏移更严重的维度，验证了"分布偏移是多维度的"这一观点。

---

## 6  相关工作

（保留v4内容，待补充更新）

---

## 7  结论与展望

本文从"分布偏移"的视角重新审视了儿童语音情绪识别问题。通过定量分析儿童与成人在声学层面和预训练模型特征空间中的分布差异，本文设计并验证了两个轻量技术模块——声学校准适配器和韵律引导时序池化，以及通过增强敏感性分析验证了分布偏移与分类退化之间的定量关联。

主要结论如下：

1. **WavLM帧级特征是最大提升来源**：从传统手工特征均值池化（~35%）到WavLM帧级特征（~78%），约43pp的提升来自特征表示的质变，确认了保留时间维度的绝对必要性。

2. **Adapter和Prosody Pooling提供额外稳定增益**：两个模块合计在严格测试上带来约2.9pp的提升（78.34%→81.24%），且轻量（合计~100万可训参数），高效（无需微调WavLM的9400万参数）。

3. **增强敏感性分析揭示了重要的"不可迁移"原则**：成人SER标配的数据增强直接应用于儿童语音会导致严重的分布偏移和分类退化（FD与准确率严格负相关）。最终推荐模型不使用波形增强。这一发现对儿童SER的工程实践具有直接的指导意义。

4. **跨语言迁移显示语言偏移仍是未解决问题**：同语言内81.24% vs 跨语言最高28.15%的差距，说明当前方法在弥合语言级分布偏移方面能力有限，这是未来工作的重要方向。

5. **实验严谨性值得重视**：本文从发现数据泄露（70/30无验证集、adapter_init含测试集信息），到实施speaker-independent三路划分（train/val/test互斥），再到记录padding mask等隐性限制，形成了一套完整的实验严谨性保障流程。

**待完善事项**（v5标注）：
- 当前所有实验数值基于60/20/20历史协议，需切换至"外部8:2 + 内部val"最终协议后重跑验证
- Padding mask支持（当前零填充帧参与池化计算，可能轻微影响结果）
- 更全面的跨语言评估和多数据集验证

---

## 参考文献

（保留v4内容，待补充更新）

---

> **版本记录**：
> - v1-v3: 初始版本，三模块框架
> - v4 (2026-04-30): 第三版完整论文，加入跨语言迁移和emotion2vec+内容
> - **v5 (2026-05-03): 重大修订** — 数据划分协议改为外部8:2+内部val；模块3从方法章移至实验章（增强敏感性分析）；A3降级（非真full fine-tune）；B2删除（非真self-attention）；最终推荐模型统一为B3（81.24%）；所有实验数值标注历史协议
