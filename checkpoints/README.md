# checkpoints/ — 模型权重

训练好的 PyTorch 模型权重。AC 套件 (ac_suite_2026-06) 的 192 个实验中保存的权重。

> 每个 `best_model.pt` ~363 MB（WavLM Base 完整模型），Git 已排除。

## autodl/ — B1-B7 实验矩阵权重

AutoDL RTX 4090D 云端训练。架构：WavLM + 12层 LayerFusion + Pooling + SEMLP (~704K 可训参数)。

| Phase | 目录 | 应有 | 实有 | 完整度 |
|-------|------|------|------|--------|
| B1 | `b1/` | 27 | 3 | 仅保留代表性实验 |
| B2 | — | 18 | 0 | 零样本迁移，无训练权重 |
| B3 | `b3/` | 36 | 36 | ✅ 完整 |
| B4 | `b4/` | 54 | 55 | ✅ 完整（合并云端+本地） |
| B5 | `b5/` | 9 | 9 | ✅ 完整（从云端下载） |
| B6 | `b6/` | 30 | 30 | ✅ 完整（从云端下载） |
| B7 | `b7/` | 18 | 18 | ✅ 完整（从云端下载） |

B1 丢失的 24 个权重训练时未保存（仅保留每个数据集的代表性 pooling）；B2 零样本实验无需训练，本来就没有权重。

### JSON 结果 vs 权重

- **JSON 结果文件** 192/192 全部保存在 `results_remote/results/logs/`
- **模型权重** 151/192 保存在此处（缺 B1-24 个 + B2-18 个零样本）
- 下载命令：`python scripts/download_checkpoints.py`

## archive/ — 历史权重

| 目录 | 内容 | 协议 |
|------|------|------|
| `old_experiments/` | 11 个旧实验 (exp1~exp5b) | AC Suite 早期 (2026-05-26~27) |
| `v5_622/` | 24 个 Phase 5 权重 | 旧协议（预提取特征 + 6:2:2） |
| `last/` | 4 个早期权重 | 更早期 |
| `best_model.pt` | 孤立的早期共用权重 | 已被各 exp 子目录取代 |

旧协议权重不可与 AC 套件数值直接对比。
